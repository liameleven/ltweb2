<div class="card">
    <div class="card-header">
        <h3 class="card-title">Success Post</h3>
    </div>
    <div class="card-body">
        <div class="col-md-12">
            <div class="card card-primary">
                <form role="form" id="quickForm" novalidate="novalidate" method="post" action="">
                    <div>
                        <div>
                            <h1 style="text-align: center;">{{post.title}}</h1>
                        </div>
                        <br>
                        <div>
                            <h2 style="text-align: center;">{{post.summary}}</h2>
                        </div>
                        <br>
                        <div>
                            <p>{{{post.content}}}</p>
                        </div>
                        <br>
                    </div>
                    {{!-- <div class="input-group">
                        <label class="label">Big Category</label>
                        <div class="form-control select2 select2-danger select2-hidden-accessible"
                            data-dropdown-css-class="select2-danger" style="width: 100%;" data-select2-id="12"
                            tabindex="-1" aria-hidden="true">
                            <select name=" bigcategory" id="bigcategory">
                                {{#each bigCategories}}
                                <option value="{{bid}}">{{name}}</option>
                                {{/each}}
                            </select>
                            <div class="select-dropdown"></div>
                        </div>
                    </div>
                    <div class="input-group" id="smallcate">
                        <label class="label">Small Category</label>
                        <div class="form-control select2 select2-danger select2-hidden-accessible"
                            data-dropdown-css-class="select2-danger" style="width: 100%;" data-select2-id="12"
                            tabindex="-1" aria-hidden="true">
                            <select name="smallcategory" id="smallcategory">
                                {{#each smallCategories}}
                                <option value="{{id}}">{{name}}</option>
                            </select>
                            <div class="select-dropdown"></div>
                        </div>
                    </div> --}}
                    <div class="card-footer">
                        <button type="submit" class="btn btn-primary">Submit</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

